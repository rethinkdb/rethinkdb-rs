[package]
name = "minimal"
version = "0.1.0"
authors = ["rushmorem <rushmore@webenchanter.com>"]
publish = false

[[bin]]
name = "minimal"
path = "main.rs"

[dependencies]
reql = { path = "../.." }
reql-types = { path = "../../types" }
futures = "0.1"
