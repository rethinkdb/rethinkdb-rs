command! {
    /// Find the remainder when dividing two numbers
    ///
    /// This is equivalent to the `mod` command in the official drivers. In Rust, `mod` is a
    /// keyword.

    #[command(rem(args(number = "T")))]
}
